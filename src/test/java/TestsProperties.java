public class TestsProperties {

    static boolean executionModeParallel = true;
    static int parallelMaxThreads = 2;

    static boolean selenoidExecution = false; // run tests in selenoid instance, all credentials to selenoid will
    // be loaded from Credentials file

    static boolean selenoidIsRemote = false; // if selenoid run on other machine


}
